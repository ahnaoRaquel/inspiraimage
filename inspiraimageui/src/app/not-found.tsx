'use client'

import { Template } from "@/components";

export default function NotfoundPage(){
    return (
        <Template>
            <section>
                <h1 className="text-black text-xl text-center">Página não existe!</h1>
            </section>
        </Template>
    )
}